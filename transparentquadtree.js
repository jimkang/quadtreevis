// var transparentQuadTree = d3.geom.quadtree;

function transparentQuadTree() {
  var tree = d3.geom.quadtree();
  // tree.treeNodes = function treeNodes() {
  //   debugger;    
  // };

  return tree;
}

